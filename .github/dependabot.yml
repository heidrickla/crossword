# Weekly version-update PRs for the Python package and any GitHub Actions
# workflows. Security alerts are separate and always on.
version: 2
updates:
  - package-ecosystem: pip
    directory: /cow-finder
    schedule:
      interval: weekly
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
